# Copy this file to `.env` and adjust values for your environment.
# Only variables prefixed with NUXT_PUBLIC_ are exposed to the browser.

# Private server-side config
NUXT_API_BASE="http://localhost:3000/api"
NUXT_MEDIA_STORAGE_BUCKET="local-bucket"

# Public client-side config
NUXT_PUBLIC_API_BASE="http://localhost:3000/api"
NUXT_PUBLIC_CDN_BASE=""
NUXT_PUBLIC_ANALYTICS_KEY=""